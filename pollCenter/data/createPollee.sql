CREATE TABLE pollee (
POLLEE_ID               VARCHAR(50) NOT NULL PRIMARY KEY,
POLL_ID               	INTEGER,
IP_ADDRESS	        VARCHAR(20),
REFERRER_ID		VARCHAR(50),
BROWSER			VARCHAR(100),
HOST			VARCHAR(50)
);
